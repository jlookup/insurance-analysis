next step:

working on compare.compare_plans()
    we now have the agg to get total paid by quarter as a Series
    now concat the 3 Series together into a df and transpose them
    return the df

    next, add the compare df to the xlsx output as the first sheet ('Summary')